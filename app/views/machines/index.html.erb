<%= render 'shared/data_log_index',
           model: Machine,
           var: @machines,
           html_path: machines_path,
           csv_path: machines_path(q: params[:q], all: true, format: :csv),
           th: [
               "ID",
               "User",
               "Host",
               "IP",
               "Last day Carbon black saw this",
               "Was put into the database at",
               "Was last updated at"
           ]
%>
