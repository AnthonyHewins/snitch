<%= render 'shared/data_log_index',
           model: Whitelist,
           var: @whitelists,
           html_path: whitelists_path,
           csv_path: whitelists_path(q: params[:q], all: true, format: :csv),
           th: ["ID", "Ruby regex", "Was created on"] %>
